{
  "name": "fase-development",
  "version": "1.0.0",
  "description": "FASE - Development workspace",
  "private": true,
  "scripts": {
    "prepare": "husky install || true",
    "verificar": "cd bin && npm pack --dry-run",
    "test": "cd bin && npm test"
  },
  "devDependencies": {
    "husky": "^9.0.0"
  },
  "dependencies": {
    "marked": "^17.0.4"
  }
}

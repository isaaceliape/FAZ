{
  "name": "fase-development",
  "version": "3.2.0",
  "description": "FASE - Development workspace",
  "private": true,
  "scripts": {
    "prepare": "husky install || true",
    "verificar-pacote": "cd bin && npm pack --dry-run",
    "verificar-instalacao": "cd bin && node verificar-instalacao.js",
    "sync-agentes": "cp agentes/*.md bin/agentes/",
    "build-docs": "node build-docs.js",
    "test": "cd bin && npm test"
  },
  "devDependencies": {
    "husky": "^9.0.0"
  },
  "dependencies": {
    "marked": "^17.0.4"
  }
}
